version: '3'
services:
  #************Eureka***************
  eureka:
    container_name: eureka_Container
    build:
      context: ./Eureka
      dockerfile: Dockerfile
    ports:
      - 8761:8761
    networks:
      - asmd_net
#************email***************
  email:
    container_name: email_Container
    build:
      context: ./EmailService-1
      dockerfile: Dockerfile
    ports:
      - 8088:8088
    networks:
      - asmd_net
    depends_on:
      - eureka  
networks:
  asmd_net:
    driver: bridge